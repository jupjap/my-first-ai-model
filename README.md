# My First AI Model

This is a simple machine learning project for the IT Career Switch course.

## What it does
- Loads the Iris dataset (built-in dataset from scikit-learn)
- Trains a Decision Tree classifier
- Makes a sample prediction
- Prints the accuracy

## How to run
```bash
pip install -r requirements.txt
python model.py
